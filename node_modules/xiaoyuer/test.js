var test = require('./weixin');

    for (var i = 0; i < 10; i += 1) {
        test.reply_settings.append(JSON.stringify( { id: 100, type: 'text', content: '点击菜单' }))
        console.log(test.reply_settings.get_reply);
    }
